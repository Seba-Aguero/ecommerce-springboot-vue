#Application config
spring.application.name=${APP_NAME}

#Database config
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.hibernate.ddl-auto=update

#Server config
server.port=${SERVER_PORT}

#Output config
spring.output.ansi.enabled=always

#JWT config
jwt.secret-key=${JWT_SECRET_KEY}
jwt.expiration=${JWT_EXPIRATION_TIME}

#Email config
spring.mail.host=${EMAIL_HOST}
spring.mail.port=${EMAIL_PORT}
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

#Order config
app.order.shipping-cost=${SHIPPING_COST}

# File upload config
file.upload.directory=src/main/resources/static/uploads/products
file.upload.baseUrl=/api/v1/products/images
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# See SQL queries
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Logging configuration
# logging.level.root=INFO
# logging.level.org.springframework.web=INFO
# logging.level.org.hibernate=ERROR
# logging.level.ecommerce_springboot_vue=DEBUG

# Configure logging pattern to be more readable
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
